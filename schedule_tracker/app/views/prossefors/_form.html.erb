<%= form_for(prossefor) do |f| %>
  <% if prossefor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prossefor.errors.count, "error") %> prohibited this prossefor from being saved:</h2>

      <ul>
      <% prossefor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :f_name %>
    <%= f.text_field :f_name %>
  </div>

  <div class="field">
    <%= f.label :l_name %>
    <%= f.text_field :l_name %>
  </div>

  <div class="field">
    <%= f.label :department %>
    <%= f.text_field :department %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
